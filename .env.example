# Copy to .env and change every value. .env is gitignored; never commit it.
MINIO_ROOT_USER=passport-admin
MINIO_ROOT_PASSWORD=change-me-to-a-long-random-string
MLFLOW_BUCKET=mlflow
# Leave empty to allow unauthenticated uploads (local use only).
PASSPORT_REGISTRY_TOKEN=
